export interface ChatHistory {
    id: string;
    title: string;
    message?: string;
    timestamp: string;
    preview: string;
    timeStamp?: string;
    model: string;
    // Add other properties as needed
}